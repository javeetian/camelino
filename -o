/**
 * @file -o
 * @brief Auto-generated Camelino C Stub Wrapper (Phase 6.9)
 *
 * Generated from: /Users/tianjianwei/Documents/github/camelino/tools/test_stub.c
 * Generated by:  camelino-stub wrap
 *
 * Include this file in your Arduino sketch to register the C primitives
 * that the OCaml bytecode expects.
 */

#ifndef CAMELINO_STUBS_H
#define CAMELINO_STUBS_H

#ifdef __cplusplus
extern "C" {
#endif

/* ---- C Stub Source (inlined for Arduino single-TU compilation) ---- */

/* === Begin test_stub.c === */
/**
 * @file test_stub.c
 * @brief 示例 Camelino C Stub — 供 Phase 6.9 验证
 *
 * 实现两个 OCaml external 声明:
 *   external add_ints : int -> int -> int = "caml_test_add"
 *   external greet    : string -> string    = "caml_test_greet"
 *
 * OCaml 端用法:
 *   let _ = add_ints 3 5          (* → 8 *)
 *   let _ = greet "world"         (* → "Hello, world!" *)
 */

#include <string.h>
#include <stdio.h>

/* ---- 依赖 Camelino runtime headers ---- */
/* #include "value.h"   -- 在设备端编译时自动包含 */
/* #include "alloc.h"   -- 在设备端编译时自动包含 */

/* OCaml value 类型 (设备端由 platform.h 提供) */
#ifndef Val_long
typedef intptr_t value;
#define Val_long(x)   (((value)(x) << 1) | 1)
#define Long_val(v)   ((intptr_t)(v) >> 1)
#define Val_bool(b)   Val_long((b) ? 1 : 0)
#define Bool_val(v)   (Long_val(v) != 0)
#define Is_long(x)    (((x) & 1) != 0)
#define Is_block(x)   (((x) & 1) == 0)
#endif

/* ---- C primitives ---- */

/* caml_test_add : int → int → int */
value caml_test_add(value a, value b) {
    return Val_long(Long_val(a) + Long_val(b));
}

/* caml_test_greet : string → string
 * 拼接 "Hello, " + 输入字符串 (简化版，不处理 OCaml string 布局) */
value caml_test_greet(value s) {
    (void)s;
    /* 在完整的 Camelino 运行时中，这里会调用 caml_alloc_string 等 */
    /* 此处为演示桩 */
    return Val_long(0);
}

/* ---- Registration helper ----
 * 在实际设备端编译时，这些函数通过 ffi.c 的
 * caml_register_primitive() 注册。
 *
 * 在 Arduino .ino 文件中:
 *   #include "camelino_stubs.h"
 *   void setup() {
 *       ...
 *       caml_register_primitive("caml_test_add", caml_test_add, 2);
 *       caml_register_primitive("caml_test_greet", caml_test_greet, 1);
 *       ...
 *   }
 */
/* === End test_stub.c === */

/* ---- Auto-registration ---- */

/**
 * Register all C primitives from this stub.
 * Call this ONCE after caml_init_primitives().
 */
static inline void camelino_register_stub_primitives(void) {
}

#ifdef __cplusplus
}
#endif

#endif /* CAMELINO_STUBS_H */
